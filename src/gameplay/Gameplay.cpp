#include "Gameplay.h"

void Gameplay::start()
{
  // handle new players
  // create rooms on join
  // handle each event (dispatch ? sub ?)
  // Each room must be able to send events, they have access to the events?
  // Can they sub somehow? If they sub what does it mean? Sub means it's pushed
  // so ...
  // No sub
}
